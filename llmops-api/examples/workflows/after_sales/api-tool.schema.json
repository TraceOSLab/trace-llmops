{
  "server": "http://127.0.0.1:8765",
  "description": "固定售后测试规则，无第三方依赖，仅用于测试",
  "paths": {
    "/policy": {
      "get": {
        "operationId": "get_test_return_policy",
        "description": "查询固定售后测试规则",
        "parameters": [
          {
            "name": "category",
            "description": "standard为有效类型，missing用于错误测试",
            "required": true,
            "type": "str",
            "in": "query"
          }
        ]
      }
    }
  }
}
